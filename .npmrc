# Expo and React Native resolve Babel and Metro plugins by walking node_modules, which pnpm
# strict linking breaks: bundling fails with "Cannot find module @babel/plugin-transform-react-jsx"
# and similar, one plugin at a time. Expo documents a hoisted linker for pnpm monorepos.
node-linker=hoisted

# Malware gate (2026-09 incident). Delegates to the shared hook so the rule lives
# in one place: ~/.config/git/hooks/post-checkout -> ~/.local/bin/git-malware-scan
[ -x "$HOME/.config/git/hooks/post-checkout" ] && exec "$HOME/.config/git/hooks/post-checkout" "$@"
exit 0

# Backlog — nabd

`NBD-N` are planning IDs; each becomes a **GitHub issue** when picked up, and branches/commits
reference that issue number. Tickets are roughly in dependency order — inner infrastructure
first. Every ticket has one acceptance criterion. The core of the MVP is **NBD-7 (daily wird
checklist)** and **NBD-8 (statistics)**.

| ID     | Status  | Ticket                                                                             | Acceptance criterion                                                                                                                |
| ------ | ------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| NBD-1  | ✅ done | **Bootstrap** — run `docs/stack.md` §1; define scripts; green baseline             | `pnpm lint && pnpm typecheck && pnpm test && pnpm build` all exit 0 on a fresh clone                                                |
| NBD-2  | ✅ done | **Feature-folder structure + import-boundary lint** (`docs/architecture.md`)       | Folder skeleton exists; a deliberate `logic.ts → db.ts` import makes `pnpm lint` fail                                               |
| NBD-16 | ✅ done | **CI workflows** (ADR-0005) — six GitHub Actions checks                            | `lint`, `typecheck`, `test`, `e2e`, `build`, `colocated-test-check` each run as a required check on PRs to `dev`/`master`           |
| NBD-3  | ✅ done | **Import design tokens from Claude Design** (ADR-0007) via `claude_design` MCP     | `DESIGN_SYSTEM.md` value tables + `app/globals.css` `@theme` hold the real palette, fonts, and scales; no placeholder tokens remain |
| NBD-4  | ✅ done | **Supabase Auth (OAuth) + session**                                                | A user can sign in with the OAuth provider and a session persists across reloads                                                    |
| NBD-5  | ✅ done | **Dexie schema + local↔Supabase sync scaffolding** (needs ADR-0006 accepted)       | A record written offline in Dexie appears in Supabase after reconnect, and vice versa                                               |
| NBD-6  | ✅ done | **Onboarding questionnaire + levels** (2 of 4 at launch)                           | A new user answers the questionnaire and lands on a daily wird sized to the chosen level                                            |
| NBD-7  | ✅ done | **Daily wird checklist** (core)                                                    | The user checks off a wird item and it stays checked after an offline reload                                                        |
| NBD-8  | ✅ done | **Statistics + drill-down** (core; needs ADR-0006)                                 | A checked-off item is reflected in the matching statistic; a past day's stats never change when the wird version changes            |
| NBD-9  | ✅ done | **Dhikr counter** (connected to the checklist)                                     | Finishing the counter for a dhikr auto-marks its linked wird item complete                                                          |
| NBD-10 | ✅ done | **Today summary** (done vs. remaining)                                             | The summary counts always match the current checklist state                                                                         |
| NBD-11 | ✅ done | **Observability** — Sentry + Vercel Analytics setup                                | A thrown error appears in Sentry with full detail; page views appear in Vercel Analytics; keys are in `.env.example`                |
| NBD-12 | ✅ done | **Adhkar library**                                                                 | The user can browse the adhkar reference without an entry existing in their wird                                                    |
| NBD-13 | ✅ done | **Intentions library** (نوايا per deed)                                            | The user can open the intention text for a given deed                                                                               |
| NBD-14 | ✅ done | **SEO basics** — per-page meta/OG, sitemap, robots.txt, semantic landing page      | Each public page has meta/OG tags; `/sitemap.xml` and `/robots.txt` resolve; the landing page renders server-side                   |
| NBD-15 | ✅ done | **PWA** — `manifest.ts` + Serwist SW + deliberate update notifier                  | App is installable; offline reload works; a new SW shows an "update available" prompt instead of auto-updating                      |
| NBD-17 | ✅ done | **Reconfigure Vercel project** for the single Next.js app (root dir, framework)    | Vercel builds from repo root; `dev` → staging and `master` → production per ADR-0003                                                |
| NBD-18 | ✅ done | **Fix sub-AA `muted-foreground` contrast** in the Claude Design project, re-import | `muted-foreground` on `bg` meets body AA (≥ 4.5) in both Classic and Modern; re-imported into `globals.css`                         |

## R2 — post-MVP update round (2026-07-15 intake)

Owner-requested updates, split into tickets. Order below is the build order (dependencies
inline). New design decisions live in ADR-0008 (schedules/optional items) and ADR-0009
(prayer times & notifications).

| ID     | Status  | Ticket                                                                                                                                                                                                                                                   | Acceptance criterion                                                                                                                                  |
| ------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| NBD-19 | ✅ done | **Tighten global spacing** — kill the oversized top/around padding; mobile-first, still balanced on desktop                                                                                                                                              | Header sits near the top on mobile (no large blank band); no horizontal crowding on desktop                                                           |
| NBD-20 | ✅ done | **Visual today-progress** — replace "إنجاز ٤ من ٩" text with a progress ring (numbers inside)                                                                                                                                                            | The ring fills to done/required and always matches the checklist state                                                                                |
| NBD-21 | ✅ done | **Checklist area accordions** — area name (start) + ٤/٥ count (end); open by default, collapsible                                                                                                                                                        | Collapsing an area hides its items but keeps its header count live; state survives toggling                                                           |
| NBD-22 | ✅ done | **Bottom navbar + pages** — stats (start) / home (center) / libraries (end); `/stats` page; libraries hub; persistent back header on sub-pages                                                                                                           | Stats render on their own page; every sub-page can navigate back without scrolling; navbar fixed on mobile                                            |
| NBD-23 | ✅ done | **Adhkar library v2** — categories as accordions + full islambook content (صباح/مساء + بعد الصلاة/نوم) with per-dhikr فضل; scraped via script                                                                                                            | Each category expands/collapses; every dhikr shows text, count, and فضل/source; content generated by `scripts/scrape-adhkar.mjs`                      |
| NBD-25 | ✅ done | **Wird schedules & optional items** (ADR-0008) — `schedule` (daily / monthly-goal / weekdays) + `optional`/`minimum`; stats & summary aware                                                                                                              | A monthly-goal item shows month progress; a weekdays item appears only on its days; optional items never lower required completion                    |
| NBD-26 | ✅ done | **Levels revamp (3 levels)** — L1 + five adhkar counters ×10 + تطوّع (قيام ١، ضحى ٢، صيام ٣/شهر); L2 (جماعة، رواتب، أذكار بعد الصلاة، حزب، ×٥٠، قيام ٣، ضحى ٢، صيام ٣/شهر); L3 جديد (+غير الرواتب، جزء، ×١٠٠، صيام الإثنين والخميس); onboarding offers 3 | A new user can land on any of the three levels and sees the right items incl. تطوّع and periodic صيام (needs NBD-25)                                  |
| NBD-27 | ✅ done | **Prayer times** (ADR-0009) — adhan.js + location permission; time beside each prayer; live non-collapsing sub-header (٣٠-min windows incl. الشروق)                                                                                                      | With location granted, each prayer shows its time and the sub-header ticks through أذّن منذ/باقي على correctly; denied ⇒ quiet prompt, nothing breaks |
| NBD-28 | ✅ done | **Prayer notifications** (ADR-0009) — onboarding permission + prefs (قبل بربع ساعة / عند الأذان / عند الإقامة، بأوقات الإقامة الثابتة); distinct foreground sounds                                                                                       | Opted-in user gets the chosen notifications while the app is open; sounds differ per moment; all limits stated in UI copy (needs NBD-27)              |
| NBD-29 | ✅ done | **Wird ↔ adhkar linking** — أذكار الصباح/المساء items link to their library category; checking adhkar one-by-one in the library auto-marks the wird item when complete                                                                                   | Both paths work: direct check-off on home, or completing all adhkar in the library marks the home item (needs NBD-23 + NBD-26)                        |
| NBD-24 | ✅ done | **Intentions library v2** — curated additions from كتاب "نوايا" (طلال فاخر) with attribution; keep the disclosure list                                                                                                                                   | The library carries the expanded curated set with a source note; each deed still opens to its intention                                               |

Deferred from this round: أذكار الصلاة ↔ wird linking (five instances per day — revisit after
NBD-29); custom notification sounds + push backend (after ADR-0009's future upgrade);
calculation-method picker for prayer times.

## R3 — owner design mockups (2026-07-16 intake, see docs/product/design-notes-r3.md)

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                                                                                                                        | Acceptance criterion                                                                                                      |
| ------ | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| NBD-29 | ✅ done | **Adhkar tabs + guided counter flow + wird linking** (design §4) — tabs per category; active dhikr card counts taps to its target then auto-advances; 3-card upcoming strip (strip scrolls, page doesn't); finishing a tab auto-marks the wird item; wird item deep-links to its tab                                                                                          | Completing all أذكار الصباح in the flow marks the home item; the home item links to the tab; direct check-off still works |
| NBD-30 | ✅ done | **Onboarding intro screen** — first step explains the app: تتبع العبادات والمحاسبة، مواقيت الصلاة، الإحصائيات                                                                                                                                                                                                                                                                 | A new user sees the purpose screen before the questionnaire                                                               |
| NBD-31 | ✅ done | **Stats v2 + export** (design §6) — streak card, weekly bars, percentage tiles; export a week or month of the user's data                                                                                                                                                                                                                                                     | Stats page matches the design language; a week/month export downloads the user's data                                     |
| NBD-32 | ✅ done | **Wird completion celebration** (design §7) — full-screen إنجاز view when required completion hits ١٠٠٪, once per day, with streak + share                                                                                                                                                                                                                                    | Completing the last required item shows the celebration exactly once for that day                                         |
| NBD-24 | ✅ done | **Intentions library v2** — curated additions from كتاب "نوايا" (طلال فاخر) with attribution; page stays a fixed list (design §5's user-added layer is Later)                                                                                                                                                                                                                 | The library carries the expanded curated set with a source note                                                           |
| NBD-33 | ✅ done | **Design polish pass** (design §§1–2) — bottom nav, day-ring card, and prayers/times section aligned with the mockups' visual language                                                                                                                                                                                                                                        | Home + nav visually match the mockups' spirit (deep-teal ring card, pill nav) with tokens only                            |
| NBD-34 | ✅ done | **UI/UX engagement pass** (issue #104) — card law (surface + border + tinted shadow; surface-2 = nested wells only), khatam ornament on deep-teal surfaces, area medallions + count chips + progress hairlines, Arabic-Indic dhikr counts + tap-progress, stats/library/onboarding/celebration depth, filled nav chip                                                         | Every page shows visible card hierarchy in light and dark; all numerals Arabic-Indic; tokens only; e2e suite stays green  |
| NBD-35 | ✅ done | **Brand assets** (issue #106) — generated brand icon (khatam + pulse): favicon.ico (16/32 simplified + 48 full), PWA PNG set + maskable safe-zone variants, apple-touch-icon, scalable SVG; static OG image (generated khatam bg + Aref Ruqaa نبض wordmark) replaces the bare pulse-line OG; manifest background_color fixed to the bg token; boilerplate public SVGs removed | Tab, installed PWA, and shared link all show the nabd brand mark; gates + e2e stay green                                  |
| NBD-36 | ✅ done | **Niyyat v3 — bulleted intentions with evidence** (issue #108) — each deed carries a set of short bullet niyyat (٧١ across ١٥ deeds) per the نوايا-book approach, every evidence line fact-checked (آية/حديث + تخريج, gradings verified against dorar/islamweb); per-deed count chip; hub shows deeds + total                                                                 | Opening a deed shows its bullet intentions each with its evidence; counts match the data; gates + e2e stay green          |

## R4 — owner intake round (2026-07-16, see docs/product/design-notes-r4.md)

Per-ticket planning is explicit this round: before implementing each ticket, write down the
files touched, the intended changes, the alternatives considered, and the chosen approach.

| ID     | Status   | Ticket                                                                                                                                                                                                                                                                                                        | Acceptance criterion                                                                                                                    |
| ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| NBD-37 | ✅ done  | **Settings page + theme/mode switchers** (r4 §§1–2, issue #110) — /settings page; light/dark toggle in the home header; classic/modern switcher in settings; both persisted per device, no FOUC                                                                                                               | Toggling theme from the header and mode from settings survives a reload; html carries the right data-theme/data-mode before first paint |
| NBD-38 | ✅ done  | **مواقيت الصلاة page + calculation-method picker** (r4 §§2–3, issue #112) — dedicated prayer-times view; method picker in settings feeding adhan.js (needs NBD-37)                                                                                                                                            | The page shows today's five times with the current/next indicator; changing the method in settings changes the computed times           |
| NBD-40 | ✅ done  | **Wird restructure — rawatib + أذكار الصلاة per prayer** (r4 §6, issue #114) — rawatib as separate ordered checkboxes around each prayer; أذكار الصلاة after every prayer (wird versioning keeps old stats correct)                                                                                           | On L2/L3 the checklist lists سنة قبلية → صلاة → سنة بعدية → أذكار الصلاة in order for each prayer; past days' stats unchanged           |
| NBD-41 | ✅ done  | **Adhkar flow persistence for صباح/مساء** (r4 §7, issue #116) — per-day flow position (dhikr index + tap count) persisted in Dexie for the two once-daily categories only                                                                                                                                     | Interrupting أذكار الصباح mid-flow and reloading resumes at the same dhikr and count; بعد الصلاة/النوم keep resetting                   |
| NBD-39 | ✅ done  | **قضاء الفوائت page** (r4 §4, ADR-0010, issue #118) — five prayers with remaining qada counts; إضافة فوائت modal (سنين/شهور/أيام → days → added to every prayer); تم قضاء صلاة decrements                                                                                                                     | Adding ٣ days shows ٣ next to each prayer; one تم القضاء tap drops that prayer to ٢; state survives offline reload                      |
| NBD-42 | ✅ done  | **Three real notification sounds + push groundwork** (r4 §5, issue #120) — real recordings (اقتربت الصلاة chime / أذان الزهراني + فجر variant / إقامة مكّية) replace the WebAudio beeps; settings previews; SW push+click handlers; scheduler honors the picked calc method                                   | Foreground moments play the three distinct sounds (الفجر gets its own adhan); previews play from settings; SW shows a pushed payload    |
| NBD-43 | 🚫 won't | **Push backend** — **superseded by the native AlarmManager shell (NBD-46 / ADR-0012)** (owner decision, R6 §9): the app now delivers prayer notifications natively with the app closed, so a web-push backend is redundant. ADR-0011 marked superseded. No code to remove (tables/function were never built). | ~~obsolete — closed~~                                                                                                                   |

## R5 — sync round (2026-07-16 intake)

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Acceptance criterion                                                                                                                                                                                                                                             |
| ------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NBD-44 | ⬜ todo | **Google OAuth live** — enable the Google provider + redirect URL in the Supabase dashboard (manual, owner), then verify the full path in-app: prod sign-in, session across reloads, wird visible on a second device                                                                                                                                                                                                                                                                                                                                                                                     | A production user signs in with Google and sees the same wird and history on a second device                                                                                                                                                                     |
| NBD-45 | ⬜ todo | **Sync everything to Supabase** (needs NBD-44) — qada_events table + RLS + per-table sync mappers (ADR-0010 §3); audit of remaining local data (adhkarFlow stays device-local by design; appearance/method/notification prefs are device settings)                                                                                                                                                                                                                                                                                                                                                       | A qada debt or payment made offline appears on a second device after reconnect, like wird entries                                                                                                                                                                |
| NBD-46 | ✅ done | **Capacitor Android shell** (ADR-0012, issue #125) — remote-URL wrapper around production; exact AlarmManager prayer alarms via @capacitor/local-notifications; four sound channels (أذان + فجر variant + إقامة + تنبيه) from res/raw; native permission branches (notifications, location); web behavior unchanged                                                                                                                                                                                                                                                                                      | A debug APK builds from the repo; inside the shell the deployed web code arms the ٧٢h alarm window with the right channel sounds; gates + e2e stay green                                                                                                         |
| NBD-47 | ✅ done | **Per-item detailed stats** — **design locked in [design-notes-r6](product/design-notes-r6.md) §8; owner plan-review gate before code.** A drill-down for **every wird checklist item** (each prayer, dhikr, quran…): consistency ٪, current streak (bridges an item's absence, never breaks on a day it wasn't in the wird), misses-in-a-row, total missed vs completed. **Derive-only** from the versioned append-only model — zero new tables, zero migration; voluntary deeds show attainment-vs-target, never a miss (ADR-0008). Pairs with NBD-53 (غير الرواتب split) and NBD-54 (fasting target). | Every checklist item opens to its own history: streak, consistency, and missed count match the stored entries; dropping then re-adding an item bridges its streak and fabricates no misses; a past day's per-item numbers never move when a new version is added |

## R6 — owner intake round (2026-07-16, see docs/product/design-notes-r6.md)

Per-ticket planning before implementation (files/changes/alternatives/choice). Decisions
locked: streak **bridges** an item's absence (never breaks on a day it wasn't in the wird);
weekly fasting is a **soft target** (checkable any day); voluntary deeds never read as failure.
Build order: NBD-48 → NBD-50 → NBD-51 → NBD-52 → NBD-53 → NBD-54 → NBD-49 → NBD-47.

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                                                                                                                                                                                 | Acceptance criterion                                                                                                                                               |
| ------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| NBD-48 | ✅ done | **Geolocation fix + Settings location control** (r6 §1) — root cause found via the owner's logcat: the Capacitor «Location services are not enabled» case (device-wide GPS toggle off, distinct from app permission). Follow-up shipped: `requestCoords` returns a typed reason (services-disabled / denied / unavailable) and every enable surface (settings, prayer-times page, onboarding) shows the matching Arabic fix-it message | A user without location sees an enable button in Settings; GPS-off vs denied show different actionable messages; granting updates every prayer-times consumer live |
| NBD-49 | ✅ done | **Alarm debug panel** (r6 §7, TEMPORARY) — shipped, owner confirmed alarms fire on-device (2026-07-17), then **removed** as planned (panel + scheduleTestAlarm/listPendingAlarms helpers)                                                                                                                                                                                                                                              | Owner confirmed a real adhan notification fired from the shell; the temporary tooling is gone from the codebase                                                    |
| NBD-50 | ✅ done | **Surface the مواقيت الصلاة page** (r6 §2, amended post-review) — bottom nav becomes a **five-item pill** (المكتبات، المواقيت، الرئيسية، الإحصائيات، الإعدادات); header keeps only theme + auth; keep the status-line door                                                                                                                                                                                                             | From any page, one bottom-nav tap reaches مواقيت الصلاة and الإعدادات without granting location first                                                              |
| NBD-51 | ✅ done | **Adhkar library ↔ home sync (صباح/مساء)** (r6 §3) — checking the wird item shows the library category finished; derived on read, no new stored state                                                                                                                                                                                                                                                                                  | Checking أذكار المساء on home shows the library مساء tab finished; the in-library path still works                                                                 |
| NBD-52 | ✅ done | **بعد الصلاة/النوم → per-dhikr counter list** (r6 §4) — replace the guided single card for these two categories with a scrollable list of independent counter cards + reset each; no celebration; صباح/مساء unchanged                                                                                                                                                                                                                  | Each dhikr on the بعد الصلاة/النوم tabs is an independent counter card with reset; no auto-advance, no celebration                                                 |
| NBD-53 | ✅ done | **Split غير الرواتب into individual items** (r6 §5, pairs with NBD-47) — L3 lumped item → per-sunnah checkboxes with stable ids; self-upgrade makes a new version, past stats unchanged                                                                                                                                                                                                                                                | L3 lists the non-raatibah sunan as separate ordered items; an existing L3 user's past stats are unchanged after the auto-upgrade                                   |
| NBD-54 | ✅ done | **Weekly fasting as a soft target** (r6 §6, amends ADR-0008) — صيام الإثنين/الخميس checkable any day, target days highlighted, any fast counts, never a miss                                                                                                                                                                                                                                                                           | صيام shows every day; fasting on a non-target day counts; الإثنين/الخميس marked as target; no miss recorded for a non-fasted day                                   |

## R7 — offline native shell (2026-07-19 intake, ADR-0013)

Research verdict: the remote-URL shell (ADR-0012 §1) cannot be reliably offline — service
workers are unreliable in the Capacitor WebView (capacitor#3205) and `server.url` is a dev
feature. The APK now bundles a static export; no OTA — every native release is a rebuilt APK
(`docs/release-android.md`). Build order: NBD-55 → NBD-56 → NBD-57.

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                            | Acceptance criterion                                                                                                                                    |
| ------ | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NBD-55 | ✅ done | **Client-side auth callback** — `/auth/callback` becomes a client page calling `exchangeCodeForSession` via the browser client (Supabase SPA pattern); deletes the route handler and `lib/db/supabase/server.ts`, the only static-export blockers; web session refresh unchanged  | The Google sign-in roundtrip completes on the web build and the session survives reloads; no route handlers remain in `app/`                            |
| NBD-56 | ✅ done | **Bundled static-export shell** (ADR-0013) — `NEXT_PUBLIC_BUILD_TARGET=native` → `output: 'export'`; `webDir: 'out'`, `server.url` removed; Serwist + update prompt + Analytics off in the native build; `build:native`/`cap:sync` scripts; release checklist doc                 | An APK built via `pnpm cap:sync` cold-starts in airplane mode straight to the wird checklist with data from Dexie; the web build and e2e stay unchanged |
| NBD-57 | ✅ done | **Native OAuth deep link** — native sign-in opens the system browser (`@capacitor/browser`), Supabase redirects to `nabd://auth/callback` (intent-filter + `@capacitor/app` listener), PKCE singleton client exchanges the code; needs the redirect URL in the Supabase dashboard | Signing in inside the APK returns to the app via the deep link with the account visible in the header; web sign-in path untouched                       |

## R8 — background-alarm reliability & Play Store readiness (2026-07-20 intake)

Research verdict: a prayer/azan app is a legitimate **alarm/reminder app** under Google Play
policy, so it qualifies for `USE_EXACT_ALARM` and the battery-optimization exemption prompt —
permissions a generic app is rejected for. Cloud sync already works (native → Supabase directly,
Dexie→outbox; Vercel is web-only), so no sync work here.

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                             | Acceptance criterion                                                                                                                            |
| ------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| NBD-58 | ✅ done | **Battery-saver onboarding step** — native-only step after permissions (`@capawesome-team/capacitor-android-battery-optimization` + `lib/impure/battery.ts`, `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`); "تفعيل الحماية" opens the OS exemption dialog; also a settings re-access row | On a battery-saver OEM, after granting the exemption the adhan fires with the app force-stopped; step is hidden on web; skip still finishes     |
| NBD-59 | 🔜 next | **Play Store readiness** — switch to `USE_EXACT_ALARM` (auto-granted, Android-14 default-deny fix; `SCHEDULE_EXACT_ALARM` kept as fallback) ✅; bump `targetSdk` 34→35 via Capacitor 6→7 upgrade (mandatory since Aug 2025) — owner tasks: signing, dev account, privacy policy    | `aapt dump badging` shows `targetSdkVersion='35'` + `USE_EXACT_ALARM`; a signed AAB builds; native checklist (`docs/release-android.md`) passes |

## R9 — owner intake round (2026-07-21): location, alarms/notifications, adhkar UX

Web-safe work ships first (one PR per item, merged to `dev`); the native items (NBD-63/64/65)
bundle into the Capacitor 6→7 / targetSdk 35 upgrade (NBD-59) for a single on-device retest.

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                                                                                      | Acceptance criterion                                                                                                                      |
| ------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| NBD-60 | ✅ done | **Adhkar counting rework + wird-level dropdown** — the five daily adhkar become checkbox + counter button (kept `kind:'counter'` so `levelMatching` survives); new الأذكار اليومية library tab (independent counters, live per-level target, two-way synced); Settings level dropdown that takes effect **next day**                        | Check on home fills the tab card; count a card to target marks home + persists offline; a level change leaves today's checklist unchanged |
| NBD-61 | ✅ done | **Adhkar reminder notifications** — nudge ~30 min after Fajr iqama (أذكار الصباح) and ~30 min after Asr iqama (أذكار المساء); toggleable; Settings notifications section                                                                                                                                                                    | Opted-in user gets the two nudges at the right offsets; toggles persist                                                                   |
| NBD-62 | ✅ done | **Real pre-prayer sound** — replace the `before.mp3` ding with a voiced "اقتربت الصلاة" (free-to-use clip, owner sign-off)                                                                                                                                                                                                                  | The 15-min-before alert plays the voiced clip on native + web foreground                                                                  |
| NBD-63 | ✅ done | **Location enable-GPS flow** (native, with NBD-59) — button pops the in-app Play Services "turn on location" dialog (LocationEnablerPlugin) then requests permission, replacing the vague services-disabled dead-end                                                                                                                        | From a cold GPS-off state, one tap prompts to enable location then grants access                                                          |
| NBD-64 | ✅ done | **Silent-mode alarm** (native, with NBD-59) — Settings switch routes the adhan through parallel USAGE_ALARM channels (AlarmAudioPlugin) so it plays through silent/vibrate (no DND override)                                                                                                                                                | With the switch on and the phone silent, the adhan still sounds                                                                           |
| NBD-65 | ✅ done | **Permanent countdown notification** (native, with NBD-59, opt-in) — ongoing notification "منذ [صلاة]" for 30 min then "باقٍ على [التالية]", sunrise as a boundary; **no foreground service** (WorkManager 15-min refresh) to dodge the Android-14 FGS-type Play policy                                                                     | Opted-in user sees a live countdown notification that survives force-stop; Android-only                                                   |
| NBD-66 | ⬜ todo | **Custom wird level** — user chooses what to track vs not track. Likely "toggle items on/off from a base preset" reusing the ADR-0006 version model; mind the `levelMatching` landmine (a custom wird matches no preset → `currentLevelId` needs a `'custom'` sentinel and self-upgrade must skip it). Applies next day like a level change | User toggles items off in Settings; next day's wird reflects the choice; past stats stay correct                                          |
| NBD-67 | ⬜ todo | **Statistics export rework** — export as image (PNG card) and/or spreadsheet, filter by day / week / month; replaces today's JSON-only `useStatsExport`. Mobile file delivery needs `@capacitor/filesystem` + `@capacitor/share` (add during/after NBD-59). Format (PNG vs CSV vs .xlsx) decided with owner when scheduled                  | User picks a period + format and gets a shareable file on web download and native share sheet                                             |

## Phase C — device-test fixes (2026-07-22): owner APK feedback on the NBD-63/64/65 native round

One PR per item, merged to `dev`; master frozen. Native runtime is owner-verified on device.

| ID     | Status  | Ticket                                                                                                                                                                                                                                                                                   | Acceptance criterion                                                                                                          |
| ------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| NBD-68 | ✅ done | **Location works offline** — `getCurrentPosition` was called with no options → Capacitor default `enableHighAccuracy:false` → Android network provider (needs internet), so the button dead-ended at "activate GPS" offline. Pass `enableHighAccuracy:true` + fall back to cached coords | Airplane mode + GPS on → the location button resolves (fresh or cached), no "activate GPS" dead-end                           |
| NBD-69 | ✅ done | **Battery-exemption popup appears** — owner saw `then is not implemented on android` (stale APK, not a v7 rename: the native module wiring is already correct). Rebuild from current `dev` + harden `battery.ts` (unimplemented → warn not Sentry error, keep settings fallback)         | On a rebuilt APK, the battery button opens the OS exemption dialog; no `.then` Sentry error                                   |
| NBD-70 | ✅ done | **Hijri date + city in the countdown notification** — title line `٨ صفر ١٤٤٨ \| مدينة طوخ` + countdown body, matching the reference. Hijri via native `HijrahDate` (always current); city via keyless Arabic reverse-geocode cached locally (**relaxes ADR-0009** — owner-approved)      | The permanent notification shows a hijri+city title line and the countdown body; hijri rolls at midnight; city cached offline |
| NBD-71 | ✅ done | **Declutter settings** — regroup the 7 flat sections into 3 labeled groups (المواقيت / التنبيهات / العرض والمحتوى); components + every `data-testid` unchanged                                                                                                                           | Settings reads as 3 clear groups; existing tests/e2e still pass                                                               |

Deferred from R9: **NBD-64 silent-mode alarm** stays open pending owner on-device retest after this round.

## Later (out of scope for MVP)

- Level 4 of the questionnaire / wird difficulty (level 3 ships in NBD-26).
- **نوايا اليوم** (design §5): user-added/day-picked intentions on top of the fixed library.
- Per-user notification-sound customization (three fixed sounds ship in NBD-42).
- The rule that a prayer checked after its window auto-counts as قضاء (NBD-39 ships the
  manual ledger; the auto-flag needs its own ADR discussion).

## Notes

- **ADR-0006 (wird versioning) is a hard prerequisite** for NBD-5 and NBD-8 — settle and
  accept it before writing statistics or durable checklist persistence.
- **NBD-3 (design tokens)** is a prerequisite for every visual/UI ticket (NBD-6 onward).
- New ideas discovered mid-build → add as new rows here, never as silent scope creep.

## MVP release

All 18 tickets shipped in **v0.14.1** (release PR #68, 2026-07-15) — verified on production:
questionnaire → level-sized wird → check-off → offline reload persists → libraries render →
sitemap/robots resolve. Live login + cross-device sync additionally require the Google OAuth
provider to be enabled in the Supabase dashboard (manual, outside the repo).
